"title": {{ asset.title | jsonify }},
            "items": [{% for skema in assets | where_exp:"item", "item.path contains '/skema/'" %}
                {
                    "title": {{ skema.path | jsonify }}
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
