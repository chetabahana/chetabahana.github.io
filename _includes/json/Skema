"items": [{% assign skemas = assets | where_exp:"item", "item.path contains '/skema/'" %}{% for skema in skemas %}
 "\t\t\t\t\t\t\t\t"{
                    "title": {{ skema.title | jsonify }}
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
