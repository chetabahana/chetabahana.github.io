"title": {{ asset.title | jsonify }},
            "items": [{% for skema in assets %}{% if skema.path contains '/skema/' %}
                    {
                        "title": {{ skema.title | jsonify }}
                    }{% unless forloop.last %},{% endunless %}{% endif %}{% endfor %}
                ]
