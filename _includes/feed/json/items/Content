{% for i in (1..my_size) %}{{ my_tab }}{% endfor %}{{ my_tab }}"content": {% assign layout = my_asset.path | split: "/" | shift | pop | join: "/" | append: "/" | append: my_asset.slug | downcase | jsonify %}{{ layout }},
