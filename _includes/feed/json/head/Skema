"items": [{% assign my_path = '/skema/sequence' %}{% assign skemas = assets | where_exp:"item", "item.path contains my_path" %}{% for skema in skemas %}
                {
                    "title": {{ page.path | jsonify }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
