"items": [{% assign skemas = assets %}{% for skema in skemas %}{% assign my_path = section.title | append: '/' | append:skema.title | downcase %}
                {
                    "title": {{ my_path }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
