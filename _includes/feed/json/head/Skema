"items": [{% assign my_path = '/skema/' %}{% assign skemas = assets | where_exp:"item", "item.path contains my_path" %}{% for skema in skemas %}
                {
                    "title": {{ skema.title | jsonify }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
