"items": [{% assign skemas = assets | where_exp:"item", "item.path contains '/skema/{{ skema.title | downcase }}'" %}{% for skema in skemas %}
                {
                    "title": {{ skema.path | jsonify }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
