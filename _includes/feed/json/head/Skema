"items": [{% assign skemas = assets | where_exp:"item", "item.path contains {{ '' | append: '/skema/'}}" %}{% for skema in skemas %}
                {
                    "titles": {{ skema.title | jsonify }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
