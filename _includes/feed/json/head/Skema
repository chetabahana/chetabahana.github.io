"items": [{% assign pat = '/skema/' %}{% assign skemas = assets | where_exp:"item", "item.path contains pat " %}{% for skema in skemas %}
                {
                    "titles": {{ skema.title | jsonify }},
                }{% unless forloop.last %},{% endunless %}{% endfor %}
            ]
